# Database Configuration (Supabase PostgreSQL)
# Format: postgresql://user:password@host:port/database?connection_limit=1
# Pour Supabase, utilisez la connection string avec connection pooling (port 6543)
# ou ajoutez ?pgbouncer=true pour utiliser le pooler
DATABASE_URL="postgresql://user:password@host:5432/database?connection_limit=1"

# JWT Secret Key (générez une clé aléatoire sécurisée)
# Vous pouvez générer une clé avec: openssl rand -base64 32
JWT_SECRET="your-secret-key-here-change-this-in-production"

# Server Port (optionnel, par défaut: 3001)
PORT=3001

# Frontend URL (optionnel, par défaut: http://localhost:3000)
# En production, utilisez l'URL de votre frontend déployé
FRONTEND_URL="http://localhost:3000"

# Node Environment (optionnel)
NODE_ENV="development"

# Variables Vercel (automatiquement définies par Vercel en production)
# VERCEL=1
# VERCEL_URL=your-app.vercel.app
# FRONTEND_VERCEL_URL=https://your-frontend.vercel.app
